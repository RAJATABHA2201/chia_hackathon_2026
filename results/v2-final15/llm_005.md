I'll start by reading the current state of my writable files.Now I'll write the new design point: array dimension 16 → 32, with the ZBU granule following the row width mechanically.Edit landed. Now verifying the markers and running the compile gate.Compile is clean (`[success]`, 1 Scala source recompiled).

**Reasoning summary.** Cycles have been *exactly* 79,433 for three consecutive measured iterations (2, 3, 4). That is structural, not noise: T-A is a fixed-latency gate, and T-B's `sc_skip` reaches only `mem.read`'s enable — neither mechanism can shorten the schedule in this build, and I predicted both as cycle-flat and was right both times. Both sanctioned techniques are now on and paid for (energy 95.7 → 24.8 µJ). The diagnosis says compute bound, `exe_active = 0.8953`, lever L2 geometry — and the arithmetic backs it: 8,388,608 MACs on 256 PEs is 32,768 cycles of streaming work against 79,433 measured, i.e. the mesh pays the WS preload+compute pair (`2·DIM` cycles per `DIM³` MACs). That ratio lives in `ExecuteController.scala` and is not writable; `DIM` is. Quadrupling the PEs retires 8× the MACs per 2× the cycles with no change to the kernel, the operand count, or either sparsity mechanism.

`granule_size 16 → 32` rides along but is not a second experiment — the granule is *forced* to equal the scratchpad row, since a `SyncReadMem` read enable is one bit per row. The ZBU module is generic in `(n, w)`, so it needs no edit; its bitmap actually shrinks (half as many rows).

I hand-checked legality before spending a build: square power-of-2 mesh at 32; sp rows/bank = 2048 and acc rows/bank = 256, both powers of 2 and multiples of 32; `sp_banks = 4 ≥ 3`; Little's Law untouched at 4096; granule divides DIM; X residency still fits (X is unchanged in bytes, and a 32 B row halves the row count against 8192 available).

### ==CANDIDATES==

technique: CONFIG
change:    meshRows/meshColumns: 16 -> 32 (granule_size: 16 -> 32 follows mechanically)
rationale: cycles pinned at 79,433 for three iterations because both sparsity mechanisms are cycle-flat by wiring; exe_active=0.90 and macs_issued/256 = 32,768 say the mesh itself is the constraint
time:      better
energy:    flat
area:      worse

technique: CONFIG
change:    dma_buswidth: 128 -> 256
rationale: off_chip_bytes=327,680 over 79,433 cycles is only 4.1 B/cyc against a 16 B/cyc bus, so this buys nothing *now* — it is the correct follow-up only once a wider mesh saturates the bus
time:      flat
energy:    flat
area:      worse

technique: T-C
change:    2:4 N:M operand mux on the streaming operand in front of MacUnit
rationale: rejected on its own merits — MAC_GATED_TOTAL says the data is 93.75% zero, far sparser than 2:4, so N:M would be denser than the data and would force multiplies T-A already skips for free, with no cycle win since the issue path is in ExecuteController.scala
time:      flat
energy:    worse
area:      worse

### ==MUTATION==
technique: CONFIG
files:     generators/gemmini/src/main/scala/gemmini/SparseCraftParams.scala
change:    L2 geometry: meshRows/meshColumns 16 -> 32 (tileRows/tileColumns stay 1), giving a 32x32 = 1024-PE square WS mesh in place of 256 PEs.
           ZBU granule_size 16 -> 32 so the granule keeps tracking the scratchpad row width, which is what the SyncReadMem row read-enable can actually consume.
           Scratchpad (256 KB), accumulator (64 KB), bank counts and all DMA/queue parameters held fixed, so the measured area delta is attributable to the mesh alone.
compiled:  PASS

### ==PREDICTION==
The mesh retires `DIM³` MACs per `2·DIM`-cycle preload+compute pair, so quadrupling the PEs cuts the cycle count for the same `macs_issued = 8,388,608` by up to 4x with no change to the operand stream; `exe_active` should stay high while `cycles` drops sharply. The bound is DMA: 327,680 off-chip bytes over ~20,000 cycles needs ~16 B/cycle against a 128-bit bus, so I expect the realised win to land between 2x and 4x, and the next diagnosis to flip from compute bound to memory bound — which is the informative outcome and points the following iteration at `dma_buswidth`. `MAC_GATED_TOTAL/macs_issued` should stay at 93.75% (T-A is per-PE and geometry-invariant), and the ZBU bitmap halves in entries.

  time:   better
  energy: flat
  area:   worse
  fmax:   flat
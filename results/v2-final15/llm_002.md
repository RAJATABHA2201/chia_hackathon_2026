Compile is clean and the edit is confirmed on disk (`?? SparseCraftParams.scala`, marker line 27 reads `x_resident = 1`).

**Reasoning from the counters.** The energy budget is DRAM-dominated: `off_chip_bytes = 3,211,264` with `RDMA_BYTES_REC = 3,080,192`, i.e. 96% of off-chip traffic is inbound operand fetch. At 22 pJ/byte that is ~70.6 µJ of the measured 95.7 µJ — roughly 74% of total energy — against `macs_issued = 8.4M × 0.30 pJ ≈ 2.5 µJ` (2.6%). So the MAC term, which is what T-A attacks, has a ceiling of a few percent, while the fetch term is three quarters of the budget. The diagnosis (`exe_active = 0.39`, scratchpad-wait 1,095,453 > rs_full 601,048) says the array is starved behind scratchpad/mvin pressure rather than issue-queue depth, which is the same story from the cycle side: X is being re-staged once per K-pass. Holding all of X resident removes both the re-fetch energy and the mvin occupancy that is stalling the array. T0 capacity check: X-resident needs 2,048 rows × 16 B = 32 KB of the 256 KB scratchpad for all 32 slices, so `sp_capacity_kb = 256` backs it with large margin. This is also software-only — `hw_hash` is untouched, so elaboration and synthesis are cache hits and the iteration costs ~1/3 of an RTL move. I deliberately did **not** flip `gate_enable` in the same turn: T-A is already wired in `PE.scala` behind a Scala-level `if`, so it is one marker away, but bundling it would change `hw_hash` and make the DRAM result unattributable.

### ==CANDIDATES==

technique: CONFIG
change:    `x_resident: 0 -> 1` (SPMM_XRES: hold all 32 X slices in the scratchpad for the whole matmul)
rationale: `RDMA_BYTES_REC = 3,080,192` is 96% of off-chip traffic and ~74% of `energy_pj` at 22 pJ/byte; the re-fetch is also the mvin pressure behind scratchpad-wait 1,095,453 and `exe_active = 0.39`
time:      better
energy:    better
area:      flat

technique: T-A
change:    `gate_enable: 0 -> 1` — enable the existing zero-gated MAC in `PE.scala` (operand-A zero test, held multiplier input, accumulator bypass on `out_b`)
rationale: `MAC_GATED_TOTAL = 7,864,320` of `macs_issued = 8,388,608` — 93.8% of multiplies have a zero A operand, the largest gateable fraction this workload will ever show
time:      flat
energy:    better
area:      worse

technique: CONFIG
change:    `sp_capacity_kb: 256 -> 128` once X-residency is measured and the working set is known to fit
rationale: energy model scales per-access SRAM cost as `sqrt(max(SRAM,32KB)/256KB)`, so halving a scratchpad whose surplus X-residency has made redundant buys area and per-access energy with cycles unchanged
time:      flat
energy:    better
area:      better

### ==MUTATION==
technique: CONFIG
files:     /home/ray/chipyard/generators/gemmini/src/main/scala/gemmini/SparseCraftParams.scala
change:    Software-schedule marker `x_resident: 0 -> 1`. The kernel now stages all 32 dense-X slices into the scratchpad once and holds them for the whole matmul instead of re-mvin-ing X per K-pass. No Chisel field touched, so `hw_hash` is unchanged and the RTL markers (`gate_enable=0`, `zbu_enable=0`, `granule_size=16`, `zbu_operand=A`), `workload=dnn512` and `dense_mode=0` are reproduced exactly.
compiled:  PASS

### ==PREDICTION==
`RDMA_BYTES_REC` collapses from 3,080,192 toward the one-shot X footprint plus A, taking `off_chip_bytes` down roughly an order of magnitude (~3.2 MB → ~0.33 MB); since DRAM bytes are ~74% of `energy_pj` at 22 pJ/byte versus 0.30 pJ per MAC, `energy_pj` should fall by more than half. Removing the per-pass X mvin also frees scratchpad ports, so scratchpad-wait drops, `exe_active_fraction` rises above 0.39 and `cycles` falls. `macs_issued`, `MAC_GATED_TOTAL` and `area_um2` are unchanged — this move touches the fetch, not the datapath. `WDMA_BYTES_SENT` stays at 131,072.

  time:   better
  energy: better
  area:   flat
  fmax:   flat

This is step 1 of a two-step plan, `plan_id: xres-then-shrink`: step 1 makes the scratchpad surplus redundant by measurement, step 2 (candidate 3) reclaims that surplus as area and per-access SRAM energy. Step 1 is expected to win on its own, not to regress.
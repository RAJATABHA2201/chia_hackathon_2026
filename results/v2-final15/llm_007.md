I'll start by pulling the history to see what's already been evaluated, so I don't repeat a design.History confirms: cycles have been pinned at 79,433 since iteration 2 — T-A (iter 3) and T-B (iter 4) both bought energy only, exactly as the wiring predicts. Let me read the actual files before choosing.Now I have the full picture. `granule = row = DIM` is a **contract**, not a choice — so DIM and granule_size are mechanically coupled. Writing the config:Edit landed (`??` = the untracked file I wrote; PE.scala and SparseCraftSparsity.scala carry my T-A/T-B work forward untouched). All 9 markers well-formed. Now the compile gate:Shell is `sh`, not bash. Re-running with an explicit bash:Compile **PASS** (2 s, clean). The change is a single lever on the one axis that has been structurally pinned: `cycles` has not moved since iteration 2, and both sanctioned mechanisms are now measured and neither can move it — T-A by construction, T-B because `sc_skip` reaches only one `SyncReadMem` enable.

### ==CANDIDATES==

technique: CONFIG
change:    `meshRows`/`meshColumns`: 16 -> 32 (`granule_size`: 16 -> 32 follows mechanically, since the ZBU contract fixes granule == scratchpad row == DIM)
rationale: `macs_issued`/DIM² is the mesh-occupancy floor (32,768 cyc) and measured is 79,433 at exe_active=0.8953 — no stall left to remove, so only work-per-cycle moves `t`
time:      better
energy:    worse
area:      worse

technique: CONFIG
change:    `k_chunk`: 16 -> 64
rationale: a longer accumulator-resident K-pass cuts read-modify-write sweeps of Y through the acc SRAM, part of the ~15 µJ on-chip term the ZBU does not cover (it taps mvin only); costs no elaboration
time:      flat
energy:    better
area:      flat

technique: T-A
change:    hoist the zero-comparator out of the PE and share one per mesh ROW — in WS the streaming operand propagates horizontally, so every PE in a row sees the same `a` and needs the same gate decision
rationale: iteration 3 measured +69,754 µm² for 256 per-PE comparators (~272 µm²/PE); at DIM=32 that is 1,024 comparators, and a row-shared gate cuts it to 32 while holding `MAC_GATED_TOTAL` identical
time:      flat
energy:    flat
area:      better

### ==MUTATION==
technique: CONFIG
files:     generators/gemmini/src/main/scala/gemmini/SparseCraftParams.scala
change:    Mesh geometry 16x16 -> 32x32 (256 -> 1,024 PEs), dataflow/tiling unchanged.
           `granule_size` marker 16 -> 32: forced, not chosen — the ZBU suppresses a whole-row `SyncReadMem` read enable, so a granule IS a row and a row IS DIM elements.
           Scratchpad/acc capacities held at 256/64 KB, so rows/bank fall 4,096 -> 2,048 and the ZBU bitmap halves (16,384 -> 8,192 bits), refunding a sliver of the mesh area.
           `dma_buswidth` deliberately left at 128: 327,680 B over ~36k cycles is ~9.1 B/cyc against a 16 B/cyc bus, still not binding.
compiled:  PASS

### ==PREDICTION==
Tile ops fall ~4x from A-blocking and ~2x from X-column tiles, while coarser 32x32 blocking lets software block-skip catch fewer all-zero blocks, so `macs_issued` should **rise** to ~15–16M — but the WS preload tax is paid per tile op, so ~2,048 tile ops at ~39 cyc becomes ~480 at ~71 cyc and `cycles` should fall 79,433 -> ~36,000. `MAC_GATED_TOTAL` stays ~93.75% of the new, larger `macs_issued`; `macs_useful` is a workload constant at 524,288. T-B's skip credit shrinks by roughly a third, because a 32-wide all-zero granule is rarer than a 16-wide one (~13% vs ~35% at 6.25% density) even though each skipped read is twice as wide — that is the stated price of the coarser granule, not a broken detector.

  time:   better
  energy: worse
  area:   worse
  fmax:   worse

This is step 1 of plan_id `geom32-gateshare`. It deliberately overspends area (4x PE count) to unpin the time axis; step 2 is candidate 3, the row-shared T-A comparator, which refunds ~1,000 comparators of that area at identical `MAC_GATED_TOTAL`. T-A being already on is what makes step 1 affordable at all — with 93.75% of the 1,024 PEs isolated every cycle, mesh dynamic energy tracks `macs_useful`, not the PE count.
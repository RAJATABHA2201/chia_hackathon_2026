Change is in place and compiling.

**What the counters said.** `off_chip_bytes = 3,211,264` at the model's 22 pJ/byte is ≈70.6 MpJ of the measured 95.7 MpJ — roughly **74% of the whole energy budget**. The 8.39M MACs are ≈2.5 MpJ (~2.6%). The diagnosis (`exe_active = 0.39`, spad-wait 1,095,453 > rs_full 601,048) is the array waiting on scratchpad rows that are being re-fetched from DRAM, not a bank-conflict or a queue-depth problem. `x_resident` was still `0`, so X is re-streamed once per K-pass.

I also confirmed that `PE.scala` and `SparseCraftSparsity.scala` are already seeded with T-A and the ZBU, both compile-time-guarded by `SparseCraftRTL.gateEnable` / the marker. So the sparsity mechanisms are one marker flip away, and spending this (cheap, software-only, `hw_hash`-preserving) iteration on the 74% term before touching the 2.6% term is the right ordering. I deliberately left `sp_capacity` at 256 KB — `x_resident` needs all 32 X slices resident, so shrinking it is the *next* step, once the measurement tells me the surplus is gone.

### ==CANDIDATES==

technique: CONFIG
change:    `x_resident: 0 -> 1` (software marker, `-DSPMM_XRES`)
rationale: `off_chip_bytes = 3,211,264` is ~74% of `energy_pj = 95,673,388` at 22 pJ/byte; X re-fetch is also what the spad-wait/`exe_active=0.39` stall is waiting on
time:      better
energy:    better
area:      flat

technique: T-A
change:    `gate_enable: 0 -> 1` — enable the seeded per-PE zero-gate on `in_a` with operand isolation
rationale: `MAC_GATED_TOTAL = 7,864,320 / 8,388,608` says 94% of multiplies have a zero A operand
time:      flat
energy:    better
area:      worse

technique: CONFIG
change:    `k_chunk: 16 -> 64` — more K-blocks per accumulator-resident pass
rationale: also attacks the same DRAM term by amortising the X/B fetch over more A rows; never swept, and bounded by the 256 KB scratchpad
time:      better
energy:    better
area:      flat

Rejected without listing at length: more `sp_banks`. The prompt is explicit that it has never moved cycles here, and I have no *bounded* fraction showing conflicts bind — `spad = 1,095,453` against `cycles = 106,650` is a free-running cross-bank accumulation at 10x the cycle count, which is exactly the trap.

I picked the first because it is software-only (elaboration and synthesis are cache hits, ~1/3 the wall clock) and it targets the term that is 28x larger than the one T-A can reach. T-A is worth at most ~2.5 MpJ; this is worth ~63 MpJ. T-A is next, on top of this.

### ==MUTATION==
technique: CONFIG
files:     generators/gemmini/src/main/scala/gemmini/SparseCraftParams.scala
change:    Software schedule marker `x_resident: 0 -> 1`. Holds all 32 slices of X
           resident in the 256 KB scratchpad for the whole matmul instead of
           re-streaming X from DRAM once per K-pass. No Chisel field touched, so
           hw_hash is unchanged; `sp_capacity` deliberately held at 256 KB because
           the resident X working set needs exactly that.
compiled:  PASS

### ==PREDICTION==
`off_chip_bytes` / `RDMA_BYTES_REC` collapse by roughly an order of magnitude (3,211,264 → ~330 KB) because the X fetch stops repeating per K-pass; at 22 pJ/byte that removes ~60 MpJ of a 95.7 MpJ budget, so `energy_pj` should fall by roughly half to two-thirds. Cycles fall as the DRAM-fed scratchpad-wait stalls drain, so `exe_active_fraction` rises from 0.39. `macs_issued` and `MAC_GATED_TOTAL` should be bit-identical — this move removes fetches, not arithmetic — and `equiv_mismatches` must stay 0 since the schedule changes where operands live, not what is computed.

  time:   better
  energy: better
  area:   flat
  fmax:   flat

plan_id `xres-then-shrink`, step 1 of 2. Step 1 does not regress. Step 2, contingent on this measuring as predicted, is to re-examine `sp_capacity_kb` once the SRAM term dominates the breakdown — with the caveat that resident X may consume the surplus that earlier runs were able to delete, in which case step 2 becomes the T-A enable instead.
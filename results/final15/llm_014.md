I'll start by reading the current state of my writable files.Let me verify whether the ZBU skip signal has any consumer, and inspect the counter hook.Let me pull the measured history with areas to see which levers actually moved area.Let me read the kernel to understand its accumulator/scratchpad requirements before choosing.Confirmed the mechanism landscape. Now let me record the key measurements before editing:

**Energy model (fitted from the archive):** energy is **99.5% off-chip bytes** (84 pJ/B × 327,680 B = 27.6 µJ) + 0.14 µJ of ungated MACs. Iter 12→13 changed cycles by −82 and area by −37,592 µm² with energy *bit-identical at 27.735* — so energy has **no area term and no meaningful cycle term**.

**T-A is at its ceiling:** MAC_GATED_TOTAL = 7,864,320 / 8,388,608 = 93.75%, exactly the zero fraction of the streaming operand. It buys 2.123 µJ (7.7% of energy) for ~23.4k µm². Iter 12 (v3→v4) recovered only **280 µm²** — PE.scala is exhausted.

**T-B is structurally dead, and I verified the wiring:** `io.sc_zbu_skip` is an `Output` of `ScratchpadBank` with **no consumer anywhere in the tree**, and `CounterFile` declares only `MAC_GATED_TOTAL = 8` — there is no ZBU counter. The hook only does `mem.read(raddr, ren && !sc_skip)`. It cannot remove a mesh push and cannot remove a modelled joule. Iter 10 measured exactly that: 0 cycles, 0 µJ, +51,114 µm².

That leaves **area** as the only objective axis with an untapped lever, and the archive prices it precisely: sp_capacity 128→64 KB removed **329,164 µm²** for 64 KB, i.e. ~5.0 µm²/byte of macro. The accumulator is the last un-harvested SRAM.Edit landed (`??` = untracked-and-present for `SparseCraftParams.scala`; `PE.scala`'s ` M` is the pre-existing harness-seeded T-A v4, which I deliberately left untouched). Let me verify the markers and the changed value, then compile.All 9 markers present and well-formed, `acc_capacity = CapacityInKilobytes(32)`. The compile failed only because the shell is `sh` (no `source`). Re-running under bash:Clean compile — `[success] Total time: 3 s`.

## Summary

I pulled the real files and the full archive before choosing, and the diagnosis (`unclassified, exe_active=0.76, dma_wait=0.02`) resolved into a clear picture once I fitted the measured history:

**Both sanctioned techniques are at their measured ceiling, not badly tuned.**

- **T-A** is saturated on *coverage*: `MAC_GATED_TOTAL / macs_issued = 7,864,320 / 8,388,608 = 93.75%`, which is exactly the zero fraction of the streaming operand (`macs_useful = 6.25%`). There is no additional zero to find. It is also saturated on *area*: iter 8 (hold-registers → stateless mux) returned 49,073 µm², but iter 12 (v3→v4) returned only **280 µm²**. The remaining block is ~23.4k µm² buying 2.123 µJ — a 7.7% energy credit for 0.77% of area. Kept verbatim.
- **T-B** is structurally dead, and I re-verified the wiring rather than trusting the note: `io.sc_zbu_skip` is an `Output` of `ScratchpadBank` (:110, driven :195) with **no consumer in the tree**, and `CounterFile` declares only `MAC_GATED_TOTAL = 8`. Its sole effect is `mem.read(raddr, ren && !sc_skip)` — it suppresses an SRAM read, so it cannot remove a mesh push (no cycle) and feeds no counter (no modelled joule). Iter 10 measured exactly that: 0 cycles, 0 µJ, +51,114 µm².

**The energy axis is closed.** Iter 12→13 moved cycles by −82 and area by −37,592 µm² with energy **bit-identical at 27.735 µJ**. So energy has no area term and no material cycle term; fitting the rest gives `E ≈ 84 pJ/B × off_chip_bytes + 0.27 pJ × ungated_macs`, i.e. **99.5% off-chip traffic**. The 327,680 B decompose as A once (131,072) + X twice (65,536) + Y once (131,072) against a floor of 294,912 — 10% of headroom, reachable only through `k_chunk`, which has a measured optimum at 16.

That leaves **area**, and the archive prices it: sp_capacity 128→64 KB removed 329,164 µm² for 64 KB (~5.0 µm²/byte). The accumulator is the last un-harvested macro, and `WDMA_BYTES_SENT = 131,072 = 512×64×4` proves every Y element is written back *exactly once* — which pins the loop order to "outer = Y region, inner = all K" and bounds the live accumulator set at one 16-row block (4,096 B = 64 rows). 32 KB is 512 rows, 8× that.

### ==MUTATION==
```
technique: CONFIG
files:     generators/gemmini/src/main/scala/gemmini/SparseCraftParams.scala
change:    acc_capacity: CapacityInKilobytes(64) -> CapacityInKilobytes(32).
           Halves the accumulator SRAM macro (512 rows total, 256/bank at
           acc_banks=2 -- power of two, multiple of DIM=16). PE.scala T-A v4
           and the disabled ZBU are left bit-for-bit unchanged.
compiled:  PASS
```

### ==PREDICTION==
The counter I am targeting is **`area_um2`**: removing 32 KB of 64-byte-wide accumulator macro at the ~5.0 µm²/byte price the sp_capacity steps established should drop area ~165k µm², 3,036,095 → ~2.87M (−5%). Nothing else should move, because there is no path from `acc_capacity` to `off_chip_bytes`: A row-block *r* feeds only Y row-block *r* (so A is read once regardless), Y is written once so long as one region fits (512 rows does so 8×), and X residency is a *scratchpad* property governed by `x_resident`/`sp_capacity`, both untouched.

The sharp falsifier: **if `WDMA_BYTES_SENT` rises above 131,072**, my loop-order inference was wrong, the accumulator was holding more than one fully-reduced region, and energy will regress by ~84 pJ per extra byte. That is the one way this move fails, and it will be visible in a single counter.

```
time:   flat
energy: flat
area:   better
fmax:   flat
```

Not part of a coupled plan. Deliberately *not* bundled with `spad_read_delay 2→1` (worth at most ~19k µm², an order of magnitude less) or `max_in_flight_mem_reqs 64→32` (~25k µm² but gated by a Little's-Law bound whose `BW × latency` product I only know an upper limit for) — bundling either would make this iteration's area delta unattributable. Both are the sequenced follow-ups if this lands clean.